<h1>Bands</h1>

<a href="<%= new_band_url %>">Add a new band</a><br><br>

<table>
  <tr>
    <td>Name</td>
    <td>Albums</td>
  </tr>
<% @bands.each do |band| %>
  <tr>
    <td><%= link(band) %></td>
    <td><%= album_links_for_band(band).join(", ").html_safe %></td>
  </tr>
<% end %>
</table>
